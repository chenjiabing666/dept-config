spring:
  profiles:
    active:
    - dev
    
---
server:
  port: 8201
spring:
  profiles: dev
  application:
    name: config-client-dev8201
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka  # eureka的暴露地址，直接注册

---
server:
  port: 8202
spring:
  profiles: test
  application:
    name: config-client-test8201
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka  # eureka的暴露地址，直接注册